{
  "name": "leetcode-habit",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && $npm_execpath run build:content && $npm_execpath run build:background && $npm_execpath run build:inject",
    "build:content": "tsc && vite build --config vite.content.config.ts",
    "build:background": "tsc && vite build --config vite.background.config.ts",
    "build:inject": "tsc && vite build --config vite.inject.config.ts",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    "start": "web-ext run --source-dir ./dist/"
  },
  "dependencies": {
    "web-ext": "^8.2.0",
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@types/webextension-polyfill": "^0.10.7",
    "@typescript-eslint/eslint-plugin": "^7.0.2",
    "@typescript-eslint/parser": "^7.0.2",
    "eslint": "^8.56.0",
    "typescript": "^5.2.2",
    "vite": "^5.1.4"
  }
}
